[tool.poetry]
name = "CatAPIIntegrator"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "AI Mofa Bot Generator <mofa-bot@moxin.com>",
]
packages = [{ include = "cat_api_integration" }]

[tool.poetry.dependencies]
python = ">=3.10"
requests = "*"

[tool.poetry.scripts]
CatAPIIntegrator = "cat_api_integration.main:main"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"