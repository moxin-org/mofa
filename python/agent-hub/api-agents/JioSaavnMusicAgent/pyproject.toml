[tool.poetry]
name = "JioSaavnMusicAgent"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "AI Mofa Bot Generator <mofa-bot@moxin.com>",
]
packages = [{ include = "jiosaavn_music" }]

[tool.poetry.dependencies]
python = ">=3.10"
requests = "*"

[tool.poetry.scripts]
JioSaavnMusicAgent = "jiosaavn_music.main:main"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"