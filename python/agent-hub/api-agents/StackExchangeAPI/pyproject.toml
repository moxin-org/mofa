[tool.poetry]
name = "StackExchangeAPI"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "AI Mofa Bot Generator <mofa-bot@moxin.com>",
]
packages = [{ include = "stack_exchange_api" }]

[tool.poetry.dependencies]
python = ">=3.10"
requests = "*"

[tool.poetry.scripts]
StackExchangeAPI = "stack_exchange_api.main:main"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"