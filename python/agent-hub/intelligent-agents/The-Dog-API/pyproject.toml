[tool.poetry]
name = "The-Dog-API"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "AI Mofa Bot Generator <mofa-bot@moxin.com>",
]
packages = [{ include = "fetch_dog_images" }]

[tool.poetry.dependencies]
python = ">=3.10"
requests = "*"

[tool.poetry.scripts]
The-Dog-API = "fetch_dog_images.main:main"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
