[tool.poetry]
name = "mem0-add"
version = "0.1.1"
authors = [
    "ZongHuan Wu ",
    "Cheng Chen",
]
description = "Use Mem0 to retrieve memory"
license = "MIT License"
homepage = "https://github.com/moxin-org/mofa"
documentation = "https://github.com/mem0ai/mem0"
readme = "README.md"
packages = [{ include = "mem0_add" }]

[tool.poetry.dependencies]
pyarrow = ">= 5.0.0"
mem0ai = ">=0.1.97"
flask = "*"
[tool.poetry.scripts]
mem0-add = "mem0_add.main:main"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
